# CDC 智汇中心 - 学院信息智能聚合平台

## 📋 项目概述

**CDC 智汇中心**是一个基于微信小程序的校园信息智能聚合平台，通过 AI 多模态解析技术，自动采集、结构化并展示校园招聘、讲座、活动等信息。

### 核心理念

**做"工具"而非"平台"** —— 随手存、随手查、不打扰。

### 核心价值

- 🤖 **AI 智能采集**：自动从文本、链接、图片中提取结构化信息
- 📱 **微信小程序**：轻量级、易用的移动端体验
- 🔄 **实时同步**：与 Supabase 数据库实时同步
- 🎯 **智能去重**：自动识别并过滤重复信息
- 🎨 **现代化 UI**：基于 Tailwind CSS 的响应式设计
- 🔒 **隐私友好**：不强制注册、不收集隐私、纯自动化运行

---

## 🏗️ 技术架构

### 前端技术栈

- **框架**：Taro 4.x + React 18
- **语言**：TypeScript
- **样式**：Tailwind CSS + Sass
- **状态管理**：React Hooks
- **数据获取**：Supabase REST API

### 后端技术栈

- **数据库**：Supabase (PostgreSQL)
- **AI 服务**：DeepSeek API
- **OCR**：pytesseract (图片文字识别)
- **网页抓取**：BeautifulSoup + Jina Reader API

### 开发工具

- **构建工具**：Webpack 5
- **包管理**：npm
- **代码规范**：ESLint + TypeScript

---

## 📁 项目结构

```
infor_platform/
├── src/                          # 源代码目录
│   ├── app.tsx                   # 应用入口
│   ├── app.config.ts             # 小程序配置
│   ├── app.scss                  # 全局样式
│   ├── pages/                    # 页面目录
│   │   └── index/                # 首页
│   │       ├── index.tsx         # 页面组件
│   │       ├── index.config.ts   # 页面配置
│   │       └── index.scss       # 页面样式
│   └── utils/                    # 工具函数
│       ├── supabase-rest.ts      # Supabase REST API 客户端
│       ├── supabase.ts.example   # Supabase SDK 示例（已禁用）
│       ├── polyfill.ts           # Polyfill 补丁
│       └── supabase使用示例.tsx  # 使用示例
│
├── scripts/                      # Python 脚本目录
│   ├── ingest_multimodal.py      # 核心：多模态信息采集脚本
│   ├── cleanup_duplicates.py    # 清理重复数据脚本
│   ├── check_data.py             # 数据检查脚本
│   ├── requirements.txt          # Python 依赖
│   ├── add_insert_policy.sql     # 数据库插入策略
│   └── add_delete_policy.sql     # 数据库删除策略
│
├── config/                       # 构建配置
│   └── index.ts                  # Taro 构建配置
│
├── dist/                         # 编译输出目录（自动生成）
│
├── supabase_schema.sql           # 数据库 Schema
│
├── .env                          # 环境变量（需自行创建）
├── package.json                  # Node.js 依赖
├── tsconfig.json                 # TypeScript 配置
├── tailwind.config.js            # Tailwind CSS 配置
├── postcss.config.js             # PostCSS 配置
│
└── README.md                     # 项目文档（本文件）
```

---

## 🚀 快速开始

### 前置要求

- Node.js >= 16.x
- Python >= 3.8
- 微信开发者工具
- Supabase 账户
- DeepSeek API Key

### 1. 环境配置

#### 1.1 安装 Node.js 依赖

```bash
npm install
```

#### 1.2 安装 Python 依赖

```bash
cd scripts
pip3 install -r requirements.txt
```

#### 1.3 配置环境变量

在项目根目录创建 `.env` 文件：

```env
# DeepSeek API
deepseek_API_KEY=your_deepseek_api_key_here

# Supabase 配置
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_anon_key_here
```

### 2. 数据库初始化

#### 2.1 创建数据库表

在 Supabase SQL Editor 中依次执行：

1. **主表**：`supabase_schema.sql`（events 表）
2. **用户表**：`supabase_schema_users.sql`（users、favorites、view_history 表）

#### 2.2 配置 RLS 策略

执行以下 SQL 脚本（在 Supabase SQL Editor）：

```sql
-- 允许读取
CREATE POLICY "Allow public read access on events"
    ON events FOR SELECT TO public USING (true);

-- 允许插入（AI 采集脚本使用）
CREATE POLICY "Allow anon to insert events"
    ON events FOR INSERT TO anon WITH CHECK (true);

-- 允许删除（清理脚本使用）
CREATE POLICY "Allow anon to delete events"
    ON events FOR DELETE TO anon USING (true);
```

### 3. 开发运行

#### 3.1 启动小程序开发

```bash
npm run dev:weapp
```

#### 3.2 在微信开发者工具中打开

1. 打开微信开发者工具
2. 选择项目目录：`dist/`
3. 开始调试

### 4. 测试 AI 采集功能

```bash
# 测试文本解析
python3 scripts/ingest_multimodal.py

# 检查数据
python3 scripts/check_data.py

# 清理重复数据
python3 scripts/cleanup_duplicates.py --yes
```

---

## 🎯 核心功能

### 1. 信息展示

- **首页 Feed**：展示所有活动、招聘、讲座信息
- **分类筛选**：按类型筛选（全部/实习招聘/讲座活动）
- **详情页**：查看完整信息，包括关键信息、摘要、原始内容
- **搜索功能**：搜索职位、公司或活动

### 2. AI 智能采集

#### 2.1 支持的数据源

- **文本输入**：直接输入群消息、通知文本
- **链接输入**：自动抓取网页/公众号文章内容
- **图片输入**：OCR 识别海报图片中的文字

#### 2.2 信息提取

AI 自动提取以下结构化信息：

```json
{
  "title": "活动或招聘标题",
  "type": "recruit | activity | lecture",
  "source_group": "发布来源",
  "key_info": {
    "date": "活动日期",
    "time": "具体时间",
    "location": "地点",
    "deadline": "截止日期"
  },
  "tags": ["标签1", "标签2"],
  "summary": "一句话摘要"
}
```

#### 2.3 智能去重

- **标题标准化**：去除括号、分隔符等变体
- **相似度匹配**：80% 相似度阈值识别重复
- **时间窗口**：检查最近 7 天内的重复数据

### 3. 数据管理

- **自动去重**：插入前自动检查重复
- **批量清理**：一键清理历史重复数据
- **数据验证**：检查数据完整性和一致性

---

## 📊 数据库设计

### Events 表结构

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGSERIAL | 主键 |
| title | TEXT | 标题 |
| type | TEXT | 类型：recruit/activity/lecture |
| source_group | TEXT | 发布来源 |
| publish_time | TEXT | 发布时间 |
| tags | TEXT[] | 标签数组 |
| key_info | JSONB | 关键信息（日期、时间、地点等） |
| summary | TEXT | 摘要 |
| raw_content | TEXT | 原始内容 |
| is_top | BOOLEAN | 是否置顶 |
| status | TEXT | 状态：active/inactive/archived |
| poster_color | TEXT | 海报颜色 |
| created_at | TIMESTAMPTZ | 创建时间 |
| updated_at | TIMESTAMPTZ | 更新时间 |

### 索引优化

- `idx_events_type`：按类型查询
- `idx_events_status`：按状态查询
- `idx_events_is_top`：置顶排序
- `idx_events_created_at`：时间排序
- `idx_events_key_info`：JSONB GIN 索引

---

## 🔧 开发指南

### 添加新页面

1. 在 `src/pages/` 下创建新目录
2. 创建 `index.tsx`、`index.config.ts`、`index.scss`
3. 在 `src/app.config.ts` 中注册页面

### 修改 AI Prompt

编辑 `scripts/ingest_multimodal.py` 中的 `SYSTEM_PROMPT`

### 自定义样式

- 全局样式：`src/app.scss`
- 页面样式：`src/pages/*/index.scss`
- Tailwind 配置：`tailwind.config.js`

### API 调用

使用 `src/utils/supabase-rest.ts` 中的函数：

```typescript
import { getEvents, getEventById } from '../../utils/supabase-rest'

// 获取所有活动
const { data, error } = await getEvents({ type: 'recruit' })

// 获取单个活动
const { data, error } = await getEventById(1)
```

---

## 🐛 常见问题

### 1. Supabase 连接失败

**问题**：小程序中显示 "url not in domain list"

**解决**：
1. 在微信公众平台配置服务器域名
2. 添加 Supabase 域名到 request 合法域名列表
3. 参考 `微信小程序域名配置说明.md`

### 2. 编译错误：Supabase SDK 模块未定义

**问题**：`module 'prebundle/vendors-node_modules_taro_weapp_prebundle_supabase_supabase-js_js.js' is not defined`

**解决**：
- 已使用 `supabase-rest.ts` 替代 SDK
- 确保没有导入 `supabase.ts`
- 参考 `替代方案说明.md`

### 3. Python 脚本无法连接数据库

**问题**：RLS 策略阻止插入/删除

**解决**：
1. 检查 `.env` 文件配置
2. 在 Supabase 控制台执行对应的 SQL 策略
3. 参考 `scripts/add_insert_policy.sql` 和 `scripts/add_delete_policy.sql`

### 4. OCR 功能不可用

**问题**：图片解析失败

**解决**：
```bash
# macOS
brew install tesseract
brew install tesseract-lang  # 中文语言包

# 验证安装
tesseract --list-langs
```

---

## 📝 开发规范

### 代码风格

- 使用 TypeScript 严格模式
- 遵循 ESLint 规则
- 组件使用函数式组件 + Hooks
- 样式使用 Tailwind CSS 工具类

### 提交规范

- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式
- `refactor`: 重构
- `test`: 测试
- `chore`: 构建/工具

### 文件命名

- 组件文件：`PascalCase.tsx`
- 工具文件：`kebab-case.ts`
- 配置文件：`kebab-case.config.js`

---

## 🚢 部署流程

### 小程序发布

1. **编译生产版本**
   ```bash
   npm run build:weapp
   ```

2. **上传代码**
   - 在微信开发者工具中点击"上传"
   - 填写版本号和项目备注

3. **提交审核**
   - 在微信公众平台提交审核
   - 等待审核通过

4. **发布上线**
   - 审核通过后，点击"发布"
   - 小程序正式上线

### 数据库备份

定期在 Supabase 控制台执行数据库备份。

---

## 📈 开发路线图

### P0 - 核心功能（必须完成）

- [x] AI 多模态信息采集
- [x] 智能去重机制
- [x] 首页 Feed 展示
- [x] 分类筛选功能
- [ ] **数据灌入**：运行 AI 脚本填充真实数据
- [ ] **收藏功能**：创建 users 和 favorites 表，实现收藏逻辑
- [ ] **搜索功能**：实现关键词搜索（公司名、职位）

### P1 - 体验优化（重要）

- [ ] **个人中心**：OpenID 登录、我的收藏、浏览足迹
- [ ] **行动点功能**：复制链接、保存二维码、添加日历
- [ ] **UI 优化**：Skeleton 骨架屏、空状态处理、过期标识

### P2 - 运营支持（可选）

- [ ] 真机调试和功能验证
- [ ] UI 微调（根据真实数据）
- [ ] 提审发布准备

### 长期规划

- [ ] 推送通知
- [ ] 智能推荐算法
- [ ] 支持多学校/多学院
- [ ] 管理后台
- [ ] 更多数据源（邮件、RSS 等）

---

## 📊 功能状态

详细功能实现状态请查看：[FEATURE_STATUS.md](./FEATURE_STATUS.md)

完整产品需求文档请查看：[PRD.md](./PRD.md)

---

## 📄 许可证

本项目为内部项目，仅供学习和研究使用。

---

## 👥 贡献者

- 项目负责人：[您的名字]
- 开发时间：2025年12月

---

## 📞 联系方式

如有问题或建议，请通过以下方式联系：

- 项目 Issues
- 邮箱：[您的邮箱]

---

## 🙏 致谢

感谢以下开源项目：

- [Taro](https://taro.jd.com/) - 多端统一开发框架
- [Supabase](https://supabase.com/) - 开源 Firebase 替代方案
- [DeepSeek](https://www.deepseek.com/) - AI 大模型服务
- [Tailwind CSS](https://tailwindcss.com/) - 实用优先的 CSS 框架

---

**最后更新**：2025年12月4日


/** @type {import('tailwindcss').Config} */
module.exports = {
  // 指定 Tailwind 需要扫描的文件路径
  content: [
    './src/**/*.{js,jsx,ts,tsx}',
    './src/**/*.wxml', // 如果使用原生小程序语法
  ],
  // 禁用 Tailwind 的默认样式重置（小程序不支持）
  corePlugins: {
    preflight: false,
  },
  theme: {
    extend: {
      // 可以在这里扩展主题配置
      // 例如自定义颜色、间距等
    },
  },
  plugins: [],
}


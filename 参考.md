微信小程序原生代码 (Native)这份代码还原了 React 版本中的“紫色渐变”、“卡片悬浮”和“双语排版”效果。1. index.wxml (页面结构)<view class="container">
  
  <!-- 1. 沉浸式头部 (Header) -->
  <view class="header-bg">
    <!-- 背景光斑装饰 (纯 CSS 实现) -->
    <view class="blur-circle circle-1"></view>
    <view class="blur-circle circle-2"></view>
    
    <view class="user-info">
      <view class="avatar-box">
        <image class="avatar" src="{{userData.avatar}}" mode="aspectFill"></image>
      </view>
      <text class="user-name">{{userData.name}}</text>
      <view class="user-id">
        <text>{{userData.id}}</text>
      </view>
    </view>
  </view>

  <!-- 2. 核心内容区 (Content) -->
  <view class="content-wrapper">
    
    <!-- A. 核心功能区 (Grid Layout) -->
    <view class="grid-box">
      <!-- 收藏卡片 -->
      <view class="big-card" bindtap="onTapFav">
        <view class="icon-circle bg-pink">
          <!-- 这里建议用 image 或 iconfont -->
          <image class="icon" src="/assets/icons/heart.png"></image>
        </view>
        <view class="text-group">
          <text class="card-title">我的收藏</text>
          <text class="card-subtitle">FAVORITES</text>
        </view>
        <view class="status-tag">暂无收藏 / No Data</view>
      </view>

      <!-- 历史卡片 -->
      <view class="big-card" bindtap="onTapHistory">
        <view class="icon-circle bg-indigo">
          <image class="icon" src="/assets/icons/clock.png"></image>
        </view>
        <view class="text-group">
          <text class="card-title">历史记录</text>
          <text class="card-subtitle">HISTORY</text>
        </view>
        <view class="status-tag">近期足迹 / Recent</view>
      </view>
    </view>

    <!-- B. 信息与反馈区 (List Layout) -->
    <view class="list-card">
      
      <!-- 意见反馈 -->
      <view class="list-item" bindtap="onTapFeedback">
        <view class="item-left">
          <view class="icon-square bg-emerald">
            <image class="icon-sm" src="/assets/icons/message.png"></image>
          </view>
          <view class="text-col">
            <view class="title-row">
              <text class="item-title">意见反馈</text>
              <text class="item-en-title">FEEDBACK</text>
            </view>
          </view>
        </view>
        <image class="arrow-right" src="/assets/icons/chevron-right.png"></image>
      </view>

      <view class="divider"></view>

      <!-- 关于我们 -->
      <view class="list-item" bindtap="onTapAbout">
        <view class="item-left">
          <view class="icon-square bg-blue">
            <image class="icon-sm" src="/assets/icons/info.png"></image>
          </view>
          <view class="text-col">
            <view class="title-row">
              <text class="item-title">关于我们</text>
              <text class="item-en-title">ABOUT US</text>
            </view>
            <text class="item-subtitle">V 1.0.0</text>
          </view>
        </view>
        <image class="arrow-right" src="/assets/icons/chevron-right.png"></image>
      </view>

    </view>

  </view>

  <!-- Footer -->
  <view class="footer">
    <text class="footer-text">Gemini UI Design</text>
    <text class="footer-sub">PREMIUM EDITION</text>
  </view>

</view>
2. index.wxss (样式表)这里我将 Tailwind 的类名转换为了原生 CSS，重点保留了紫色渐变和阴影。/* 基础设置 */
page {
  background-color: #f8fafc; /* slate-50 */
  font-family: -apple-system, BlinkMacSystemFont, 'Helvetica Neue', Helvetica, Segoe UI, Arial, Roboto, 'PingFang SC', 'miui', 'Hiragino Sans GB', 'Microsoft Yahei', sans-serif;
  padding-bottom: 40px;
}

/* --- 头部区域 --- */
.header-bg {
  position: relative;
  width: 100%;
  padding-top: 48px;
  padding-bottom: 96px; /* 留出空间给下面卡片上浮 */
  /* 紫色渐变核心代码 */
  background: linear-gradient(135deg, #7c3aed 0%, #9333ea 50%, #c026d3 100%);
  border-bottom-left-radius: 40px;
  border-bottom-right-radius: 40px;
  overflow: hidden;
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
}

/* 头部装饰光斑 */
.blur-circle {
  position: absolute;
  border-radius: 50%;
  filter: blur(64px); /* blur-3xl */
  opacity: 0.15;
}
.circle-1 {
  width: 200px;
  height: 200px;
  background: white;
  top: -50px;
  left: -50px;
}
.circle-2 {
  width: 250px;
  height: 250px;
  background: #c084fc;
  bottom: 0;
  right: 0;
  opacity: 0.2;
}

/* 用户信息 */
.user-info {
  position: relative;
  z-index: 10;
  display: flex;
  flex-direction: column;
  align-items: center;
  color: white;
}

.avatar-box {
  width: 80px;
  height: 80px;
  border-radius: 50%;
  padding: 4px;
  background-color: rgba(255, 255, 255, 0.2);
  backdrop-filter: blur(4px);
  margin-bottom: 12px;
}

.avatar {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  background-color: #f1f5f9;
}

.user-name {
  font-size: 20px;
  font-weight: 700;
  letter-spacing: 0.5px;
  margin-bottom: 4px;
}

.user-id {
  font-size: 12px;
  color: #e9d5ff; /* purple-200 */
  background-color: rgba(0, 0, 0, 0.1);
  padding: 2px 12px;
  border-radius: 9999px;
  border: 1px solid rgba(255, 255, 255, 0.1);
}

/* --- 内容区域 --- */
.content-wrapper {
  padding: 0 20px;
  margin-top: -64px; /* 负边距实现上浮效果 */
  position: relative;
  z-index: 20;
}

/* 大卡片 Grid */
.grid-box {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
  margin-bottom: 20px;
}

.big-card {
  background: white;
  border-radius: 16px;
  padding: 20px;
  display: flex;
  flex-direction: column;
  align-items: center;
  box-shadow: 0 4px 6px -1px rgba(226, 232, 240, 0.5); /* shadow-slate-200 */
}

.big-card:active {
  transform: scale(0.98);
  transition: transform 0.1s;
}

.icon-circle {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 8px;
}
.bg-pink { background-color: #fdf2f8; color: #ec4899; }
.bg-indigo { background-color: #eef2ff; color: #6366f1; }
.bg-emerald { background-color: #ecfdf5; color: #10b981; }
.bg-blue { background-color: #eff6ff; color: #3b82f6; }

.icon { width: 26px; height: 26px; }

.text-group {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.card-title {
  font-size: 16px;
  font-weight: 700;
  color: #334155;
}

.card-subtitle {
  font-size: 10px;
  font-weight: 600;
  color: #cbd5e1;
  text-transform: uppercase;
  margin-top: 2px;
}

.status-tag {
  font-size: 12px;
  color: #94a3b8;
  background-color: #f8fafc;
  padding: 2px 8px;
  border-radius: 6px;
  margin-top: 8px;
  border: 1px solid #f1f5f9;
}

/* 列表卡片 */
.list-card {
  background: white;
  border-radius: 16px;
  box-shadow: 0 10px 15px -3px rgba(226, 232, 240, 0.6);
  overflow: hidden;
}

.list-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px;
}
.list-item:active { background-color: #f8fafc; }

.item-left {
  display: flex;
  align-items: center;
  gap: 16px;
}

.icon-square {
  width: 40px;
  height: 40px;
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.icon-sm { width: 20px; height: 20px; }

.text-col {
  display: flex;
  flex-direction: column;
}

.title-row {
  display: flex;
  align-items: baseline;
  gap: 8px;
}

.item-title {
  font-size: 14px;
  font-weight: 700;
  color: #334155;
}

.item-en-title {
  font-size: 10px;
  font-weight: 500;
  color: #94a3b8;
  text-transform: uppercase;
}

.item-subtitle {
  font-size: 10px;
  color: #94a3b8;
  margin-top: 2px;
}

.arrow-right { width: 18px; height: 18px; opacity: 0.3; }

.divider {
  height: 1px;
  background-color: #f8fafc;
  margin-left: 64px;
  margin-right: 16px;
}

/* Footer */
.footer {
  margin-top: 48px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
}
.footer-text { font-size: 12px; color: #94a3b8; }
.footer-sub { font-size: 10px; color: #cbd5e1; letter-spacing: 2px; }
3. index.js (数据逻辑)Page({
  data: {
    userData: {
      name: "Alex Design",
      id: "ID: 888888",
      avatar: "[https://api.dicebear.com/7.x/notionists/svg?seed=Alex](https://api.dicebear.com/7.x/notionists/svg?seed=Alex)"
    }
  },

  // 事件处理函数
  onTapFav() {
    console.log("点击了收藏");
    wx.navigateTo({ url: '/pages/favorites/index' });
  },

  onTapHistory() {
    console.log("点击了历史记录");
  },

  onTapFeedback() {
    console.log("点击了反馈");
  },
  
  onTapAbout() {
    console.log("点击了关于");
  }
})

# 📐 项目结构说明

本文档详细说明项目的目录结构和文件组织方式。

## 🗂️ 目录树

```
infor_platform/
│
├── 📄 README.md                    # 项目主文档（PRD 格式）
├── 📄 PROJECT_STRUCTURE.md         # 项目结构说明（本文件）
├── 📄 package.json                 # Node.js 项目配置
├── 📄 tsconfig.json                # TypeScript 配置
├── 📄 tailwind.config.js           # Tailwind CSS 配置
├── 📄 postcss.config.js            # PostCSS 配置
├── 📄 babel.config.js              # Babel 配置
├── 📄 .env                         # 环境变量（不提交到 Git）
├── 📄 .gitignore                   # Git 忽略文件
│
├── 📁 src/                         # 源代码目录
│   ├── app.tsx                     # 应用入口组件
│   ├── app.config.ts               # 小程序全局配置
│   ├── app.scss                    # 全局样式
│   │
│   ├── 📁 pages/                   # 页面目录
│   │   └── index/                  # 首页
│   │       ├── index.tsx          # 页面主组件
│   │       ├── index.config.ts    # 页面配置
│   │       └── index.scss         # 页面样式
│   │
│   └── 📁 utils/                   # 工具函数目录
│       ├── supabase-rest.ts        # Supabase REST API 客户端（主要使用）
│       ├── supabase.ts.example     # Supabase SDK 示例（已禁用，仅作参考）
│       ├── polyfill.ts             # Polyfill 补丁（URL、fetch 等）
│       └── supabase使用示例.tsx   # Supabase 使用示例代码
│
├── 📁 scripts/                     # Python 脚本目录
│   ├── ingest_multimodal.py        # ⭐ 核心：多模态信息采集脚本
│   ├── cleanup_duplicates.py      # 清理重复数据脚本
│   ├── check_data.py               # 数据检查脚本
│   ├── requirements.txt            # Python 依赖列表
│   ├── add_insert_policy.sql       # 数据库插入策略 SQL
│   └── add_delete_policy.sql      # 数据库删除策略 SQL
│
├── 📁 config/                      # 构建配置目录
│   └── index.ts                    # Taro 构建配置文件
│
├── 📁 docs/                        # 文档目录
│   ├── README.md                   # 文档索引
│   ├── Supabase集成指南.md
│   ├── Supabase连接测试指南.md
│   ├── 微信小程序域名配置说明.md
│   ├── 域名配置问题解决.md
│   ├── weapp-tailwindcss配置指南.md
│   ├── URL_Polyfill_修复说明.md
│   ├── 替代方案说明.md
│   ├── 如何找到测试按钮.md
│   └── 快速测试步骤.md
│
├── 📁 dist/                        # 编译输出目录（自动生成，不提交）
│   └── ...                         # 编译后的小程序代码
│
├── 📁 node_modules/                 # Node.js 依赖（不提交）
│
└── 📄 supabase_schema.sql          # 数据库 Schema 定义
```

## 📋 核心文件说明

### 前端核心文件

| 文件 | 说明 | 重要性 |
|------|------|--------|
| `src/pages/index/index.tsx` | 首页主组件，包含 Feed 列表、筛选、详情等功能 | ⭐⭐⭐ |
| `src/utils/supabase-rest.ts` | Supabase REST API 客户端，所有数据操作都通过它 | ⭐⭐⭐ |
| `src/utils/polyfill.ts` | Polyfill 补丁，解决微信小程序环境兼容性问题 | ⭐⭐ |
| `config/index.ts` | Taro 构建配置，包含 Webpack 配置 | ⭐⭐ |

### 后端核心文件

| 文件 | 说明 | 重要性 |
|------|------|--------|
| `scripts/ingest_multimodal.py` | AI 多模态信息采集脚本，核心功能 | ⭐⭐⭐ |
| `scripts/cleanup_duplicates.py` | 清理重复数据脚本 | ⭐⭐ |
| `scripts/check_data.py` | 数据检查工具 | ⭐ |

### 配置文件

| 文件 | 说明 |
|------|------|
| `package.json` | Node.js 项目配置和依赖 |
| `tsconfig.json` | TypeScript 编译配置 |
| `tailwind.config.js` | Tailwind CSS 配置 |
| `.env` | 环境变量（需自行创建） |

### 数据库文件

| 文件 | 说明 |
|------|------|
| `supabase_schema.sql` | 数据库表结构和索引定义 |
| `scripts/add_*_policy.sql` | RLS 策略 SQL 脚本 |

## 🔄 数据流向

```
用户输入（文本/链接/图片）
    ↓
ingest_multimodal.py (AI 解析)
    ↓
Supabase Database
    ↓
supabase-rest.ts (REST API)
    ↓
小程序前端 (index.tsx)
    ↓
用户界面
```

## 📦 依赖管理

### Node.js 依赖

- **主要依赖**：Taro、React、Supabase
- **开发依赖**：TypeScript、Tailwind CSS、Webpack
- **管理方式**：`package.json` + `npm install`

### Python 依赖

- **主要依赖**：deepseek、supabase、python-dotenv、requests、beautifulsoup4、pytesseract、Pillow
- **管理方式**：`scripts/requirements.txt` + `pip install -r requirements.txt`

## 🎯 模块职责

### 前端模块

- **页面组件**：UI 展示和用户交互
- **工具函数**：数据获取、状态管理
- **样式系统**：Tailwind CSS + 自定义样式

### 后端模块

- **采集脚本**：多模态信息解析和入库
- **清理脚本**：数据维护和去重
- **检查脚本**：数据验证和诊断

### 数据库模块

- **Schema**：表结构定义
- **RLS 策略**：安全策略配置
- **索引**：查询性能优化

## 🔧 开发工作流

1. **前端开发**：修改 `src/` 下的文件
2. **后端开发**：修改 `scripts/` 下的 Python 脚本
3. **数据库变更**：修改 `supabase_schema.sql` 并在 Supabase 执行
4. **配置调整**：修改 `config/` 下的配置文件
5. **文档更新**：修改 `docs/` 下的文档

## 📝 命名规范

- **组件文件**：`PascalCase.tsx`
- **工具文件**：`kebab-case.ts`
- **配置文件**：`kebab-case.config.js`
- **Python 脚本**：`snake_case.py`
- **SQL 文件**：`snake_case.sql`

## 🚫 不应提交的文件

- `node_modules/` - 依赖包
- `dist/` - 编译输出
- `.env` - 环境变量（包含敏感信息）
- `*.log` - 日志文件
- `微信图片_*.jpg` - 临时测试图片

---

**最后更新**：2025年12月4日






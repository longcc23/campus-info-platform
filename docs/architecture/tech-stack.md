# 🔧 UniFlow V1.1.0 - 技术栈说明

**版本**：V1.1.0  
**日期**：2025年12月

---

## ⚠️ 重要区分：两个独立的前端应用

UniFlow V1.1.0 包含**两个完全独立的前端应用**：

1. **小程序端（用户端）** - 微信小程序
2. **管理后台（管理端）** - Web 应用

它们使用不同的技术栈，服务于不同的用户群体。

---

## 📱 小程序端（用户端）

### 技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| **框架** | Taro 4.x | 跨平台小程序框架 |
| **UI 框架** | React 18 | 组件库 |
| **语言** | TypeScript | 类型安全 |
| **样式** | SCSS + Tailwind CSS | 使用 weapp-tailwindcss |
| **组件** | Taro 原生组件 | View, Text, ScrollView 等 |
| **平台** | 微信小程序 | 运行环境 |

### 项目位置

```
infor_platform/
├── src/
│   ├── pages/          # 小程序页面
│   ├── components/     # 小程序组件
│   ├── styles/         # SCSS 样式
│   └── ...
└── config/             # Taro 配置
```

### 开发命令

```bash
npm run dev:weapp      # 开发模式
npm run build:weapp    # 生产构建
```

### 特点

- ✅ 运行在微信小程序平台
- ✅ 使用 Taro 组件（不是 HTML）
- ✅ 样式使用 rpx 单位
- ✅ 需要微信开发者工具调试

---

## 🖥️ 管理后台（管理端）

### 技术栈

| 技术 | 版本 | 说明 |
|------|------|------|
| **框架** | Next.js 14 | React 全栈框架 |
| **UI 框架** | React 18 | 组件库 |
| **语言** | TypeScript | 类型安全 |
| **样式** | Tailwind CSS | 实用优先的 CSS |
| **图标** | Lucide React | 图标库（参考代码使用） |
| **组件** | Shadcn/ui | 基于 Radix UI |
| **平台** | Web 浏览器 | 桌面端优先 |

### 项目位置（新建）

```
admin-console/          # 新的独立项目
├── app/               # Next.js App Router
├── components/        # React 组件
├── lib/              # 工具库
└── ...
```

### 开发命令（新建项目）

```bash
npm run dev            # 开发模式（localhost:3000）
npm run build          # 生产构建
npm run start          # 生产模式运行
```

### 特点

- ✅ 运行在 Web 浏览器
- ✅ 使用标准 HTML/CSS
- ✅ 响应式设计（桌面端优先）
- ✅ 可在浏览器直接访问

---

## 🔄 技术栈对比

### 相同点

| 特性 | 小程序端 | 管理后台 |
|------|---------|---------|
| **UI 框架** | React 18 | React 18 ✅ |
| **语言** | TypeScript | TypeScript ✅ |
| **状态管理** | React Hooks | React Hooks ✅ |

### 不同点

| 特性 | 小程序端 | 管理后台 |
|------|---------|---------|
| **构建框架** | Taro | Next.js |
| **样式方案** | SCSS | Tailwind CSS |
| **组件库** | Taro 组件 | HTML 标签 + Shadcn/ui |
| **图标库** | Emoji / 图片 | Lucide React |
| **单位** | rpx | px / rem |
| **运行平台** | 微信小程序 | Web 浏览器 |
| **调试工具** | 微信开发者工具 | Chrome DevTools |

---

## 📐 代码示例对比

### 小程序端（Taro）

```tsx
// src/pages/index/index.tsx
import { View, Text } from '@tarojs/components'
import './index.scss'

export default function Index() {
  return (
    <View className="index-page">
      <Text>Hello Taro</Text>
    </View>
  )
}
```

```scss
// src/pages/index/index.scss
.index-page {
  padding: 24rpx;  // 使用 rpx
}
```

### 管理后台（Next.js）

```tsx
// app/(dashboard)/dashboard/page.tsx
'use client'
import { LayoutDashboard } from 'lucide-react'

export default function Dashboard() {
  return (
    <div className="p-8">  {/* 使用 Tailwind */}
      <LayoutDashboard size={24} />
      <h1>Hello Next.js</h1>
    </div>
  )
}
```

---

## 🗂️ 项目结构

### 当前项目结构（小程序端）

```
infor_platform/               # 小程序项目
├── src/
│   ├── pages/
│   │   ├── index/           # 首页
│   │   ├── favorites/       # 收藏页
│   │   ├── history/         # 浏览历史
│   │   └── profile/         # 个人中心
│   ├── components/
│   ├── styles/
│   └── utils/
├── config/
└── package.json
```

### 新建项目结构（管理后台）

```
admin-console/                # 管理后台项目（新建）
├── app/
│   ├── (dashboard)/
│   │   ├── dashboard/       # 数据看板
│   │   ├── ingest/         # AI 智能发布台
│   │   ├── events/         # 活动列表
│   │   └── layout.tsx      # Dashboard 布局
│   ├── api/
│   │   ├── ai/parse/       # AI 解析 API
│   │   └── events/         # 事件管理 API
│   └── layout.tsx
├── components/
│   ├── layout/
│   │   ├── Sidebar.tsx     # 侧边栏
│   │   └── TopNav.tsx      # 顶部导航
│   └── ingest/
│       └── IngestView.tsx  # AI 发布台
├── lib/
│   ├── ai/
│   │   └── parser.ts       # AI 解析逻辑
│   └── supabase/
└── package.json
```

---

## 🔌 数据层共享

### 共同使用 Supabase

两个前端应用都连接到同一个 Supabase 数据库：

```
小程序端                   管理后台
   │                         │
   ├─────── Supabase ────────┤
   │                         │
   └───── events 表 ─────────┘
   └───── users 表 ──────────┘
   └───── favorites 表 ──────┘
   └───── view_history 表 ───┘
```

### API 调用方式

#### 小程序端（使用 Taro.request）

```typescript
// src/utils/supabase-rest.ts
import Taro from '@tarojs/taro'

export async function getEvents() {
  const response = await Taro.request({
    url: 'https://xxx.supabase.co/rest/v1/events',
    method: 'GET',
    header: {
      'apikey': SUPABASE_ANON_KEY
    }
  })
  return response.data
}
```

#### 管理后台（使用 Supabase Client 或 Next.js API）

```typescript
// lib/supabase/client.ts
import { createClient } from '@supabase/supabase-js'

export const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
)

// 使用
const { data } = await supabase.from('events').select('*')
```

---

## 🚀 开发流程

### 小程序端开发

1. 在 `infor_platform/` 目录下开发
2. 使用 Taro 命令启动
3. 在微信开发者工具中预览

### 管理后台开发

1. 新建 `admin-console/` 项目
2. 使用 Next.js 开发
3. 在浏览器中访问 `localhost:3000`

### 数据同步

- 两个应用共享 Supabase 数据库
- 管理后台发布内容 → 小程序端自动更新（轮询或 WebSocket）

---

## 📋 开发团队分工

### A 路：小程序优化组

- 继续在 `infor_platform/` 项目工作
- 使用 Taro + SCSS 技术栈
- 优化小程序 UI 和功能

### B 路：后台搭建组

- 新建 `admin-console/` 项目
- 使用 Next.js + Tailwind 技术栈
- 参考提供的 React 示例代码
- 搭建管理后台功能

---

## ✅ 总结

### 关键点

1. ✅ **两个独立的前端应用**
2. ✅ **不同的技术栈**（Taro vs Next.js）
3. ✅ **不同的运行平台**（小程序 vs Web）
4. ✅ **共享数据层**（Supabase）
5. ✅ **参考代码是管理后台的**，不是小程序的

### 技术选型原则

- **小程序端**：必须使用 Taro（微信小程序限制）
- **管理后台**：使用 Next.js（Web 应用标准方案）
- **设计风格**：保持视觉一致性，但技术实现不同

---

**文档版本**：V1.1.0  
**最后更新**：2025年12月


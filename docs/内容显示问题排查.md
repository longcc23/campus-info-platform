# 内容显示问题排查指南

## 问题描述
添加 tabBar 后，页面内容不显示

## 排查步骤

### 1. 确认当前页面
- 点击底部导航栏的 **"首页"** 按钮
- 确保当前在首页，而不是"我的"页面

### 2. 检查控制台错误
在微信开发者工具中：
1. 点击顶部菜单 **"调试"** → **"调试器"**
2. 查看 **Console** 标签页
3. 检查是否有红色错误信息

### 3. 检查网络请求
1. 在调试器中点击 **Network** 标签
2. 刷新页面
3. 查看是否有失败的请求（红色）

### 4. 强制刷新
1. 在微信开发者工具中点击 **"编译"** 按钮
2. 或使用快捷键 `Cmd + R` (macOS)

### 5. 清除缓存
1. 点击顶部菜单 **"工具"** → **"清除缓存"**
2. 选择 **"清除全部缓存"**
3. 重新编译

## 预期结果

### 首页应该显示：
- 顶部：CDC 智汇中心 标题
- 搜索框
- 筛选标签：全部 / 实习招聘 / 讲座活动
- 3 条 Mock 数据：
  1. Google Office Tour & 2026 暑期实习预热
  2. Career BootCamp: Networking & Insights
  3. 2025年秋季学期中期实践活动招募

### 我的页面应该显示：
- 个人信息卡片（头像 + 用户 ID）
- 统计信息（收藏数 / 浏览数）
- 功能列表（我的收藏 / 浏览历史）

## 常见问题

### Q1: 页面完全空白
**原因**: 可能在"我的"页面，而不是"首页"
**解决**: 点击底部 "首页" 按钮

### Q2: 显示"加载中..."
**原因**: 页面卡在 loading 状态
**解决**: 
1. 检查控制台是否有错误
2. 检查 Supabase 配置是否正确
3. 尝试点击顶部的 🧪 按钮测试连接

### Q3: 只显示标题，没有内容
**原因**: 可能是样式问题或数据加载失败
**解决**:
1. 打开控制台查看是否有错误
2. 检查 `feed` 状态是否有数据
3. 在控制台输入: `console.log(feed)` 查看数据

## 调试技巧

### 添加调试日志
在 `src/pages/index/index.tsx` 中添加：

```typescript
// 在 Index 组件开头添加
useEffect(() => {
  console.log('=== 首页状态 ===')
  console.log('feed 数据:', feed)
  console.log('filteredFeed 数据:', filteredFeed)
  console.log('activeFilter:', activeFilter)
  console.log('searchKeyword:', searchKeyword)
}, [feed, filteredFeed, activeFilter, searchKeyword])
```

### 检查数据是否加载
在控制台输入：
```javascript
// 查看 feed 数据
console.log('feed:', feed)

// 查看过滤后的数据
console.log('filteredFeed:', filteredFeed)

// 查看 INITIAL_DATA
console.log('INITIAL_DATA:', INITIAL_DATA)
```

## 下一步

如果以上步骤都无法解决问题，请提供：
1. 控制台的完整错误信息（截图）
2. 当前显示的页面截图
3. Network 标签中的请求列表（截图）

# 🤖 AI 智能识别功能 - 快速开始

## 🚀 启动步骤

### 1. 安装 Python 依赖

```bash
cd scripts
pip install -r requirements.txt
```

### 2. 配置环境变量

确保 `.env` 文件已配置：
```
deepseek_API_KEY=your_key_here
SUPABASE_URL=your_url_here
SUPABASE_KEY=your_key_here
```

### 3. 启动 API 服务

**方式 1：使用启动脚本**
```bash
./scripts/start_api.sh
```

**方式 2：直接运行**
```bash
cd scripts
python3 api_server.py
```

服务将启动在 `http://localhost:5000`

### 4. 测试 API

```bash
# 健康检查
curl http://localhost:5000/health

# 测试文本识别
curl -X POST http://localhost:5000/api/ingest \
  -H "Content-Type: application/json" \
  -d '{
    "content": "【实习】美团-商业分析实习生-商业化战略方向（base北京）",
    "type": "text"
  }'
```

### 5. 在小程序中使用

1. 确保 API 服务正在运行
2. 打开小程序
3. 点击"智能识别助手"按钮
4. 输入内容并识别

## 📋 API 接口

### POST /api/ingest

**请求**：
```json
{
  "content": "文本/链接/图片base64",
  "type": "text" | "link" | "image_url"
}
```

**响应**：
```json
{
  "success": true,
  "message": "内容已成功处理并保存到数据库"
}
```

## ⚠️ 注意事项

1. **开发环境**：API 服务需要运行在本地
2. **生产环境**：需要部署到服务器并配置 HTTPS
3. **微信小程序**：需要在后台配置域名白名单
4. **超时设置**：AI 处理可能需要 10-30 秒，已设置 30 秒超时

---

**详细文档**：查看 `docs/AI智能识别功能实现说明.md`



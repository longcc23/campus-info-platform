# 🏗️ CDC 智汇中心 V3.0 - 架构快速参考

**版本**：V3.0  
**日期**：2025年12月

---

## 📊 架构一览

```
┌────────────────────────────────────────────────────────────┐
│                   CDC 智汇中心 V3.0                        │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────────┐          ┌──────────────┐              │
│  │  用户端 (C端)  │          │  管理端 (B端)  │              │
│  │  微信小程序    │          │  Web Dashboard│              │
│  │               │          │               │              │
│  │ ✅ 已实现     │          │ 🔲 计划中     │              │
│  │               │          │               │              │
│  │ • Taro        │          │ • Next.js     │              │
│  │ • React       │          │ • React       │              │
│  │ • SCSS        │          │ • Tailwind    │              │
│  └──────┬───────┘          └──────┬───────┘              │
│         │                         │                      │
│         └─────────┬───────────────┘                      │
│                   │                                      │
│         ┌─────────▼─────────┐                           │
│         │  Supabase (数据层)  │                           │
│         │  • PostgreSQL     │                           │
│         │  • Auth           │                           │
│         └─────────┬─────────┘                           │
│                   │                                      │
│         ┌─────────▼─────────┐                           │
│         │  AI 服务层        │                           │
│         │  • Python (当前)  │                           │
│         │  • TypeScript (迁移)│                         │
│         │  • LangChain.js   │                           │
│         └───────────────────┘                           │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 🎯 核心组件状态

| 组件 | 技术栈 | 状态 | 用户 |
|------|--------|------|------|
| **用户端 (小程序)** | Taro + React + SCSS | ✅ 已实现 | 学生 |
| **管理端 (Web)** | Next.js + React + Tailwind | 🔲 计划中 | 老师/班委 |
| **数据层 (Supabase)** | PostgreSQL + Auth | ✅ 已实现 | - |
| **AI 服务 (Python)** | Python + GPT-4 | ✅ 已实现 | 本地运行 |
| **AI 服务 (迁移)** | TypeScript + LangChain | 🔲 计划中 | 云端运行 |

---

## 🔄 数据流

### 发布流程

```
老师 → 管理后台 → AI 识别 → 人工审核 → 发布 → 小程序更新 → 学生看到
```

### 技术实现

```
管理后台 (Next.js)
    ↓
API Route (/api/ai/parse)
    ↓
LangChain.js → DeepSeek API
    ↓
返回结果 → 人工审核
    ↓
POST /api/events → Supabase
    ↓
小程序 (轮询/WebSocket)
    ↓
学生看到新内容
```

---

## 📁 项目结构

### 当前项目 (小程序)

```
infor_platform/
├── src/
│   ├── pages/          # 小程序页面
│   ├── components/     # 小程序组件
│   └── utils/          # 工具函数
└── scripts/            # Python AI 脚本
```

### 新建项目 (管理后台)

```
admin-console/          # 新建独立项目
├── app/               # Next.js App Router
├── components/        # React 组件
└── lib/              # 工具库
```

---

## 🗄️ 数据库表

### ✅ 已实现

- `events` - 活动表
- `users` - 用户表
- `favorites` - 收藏表
- `view_history` - 浏览历史

### 🔲 待创建

- `admin_users` - 管理员表
- `audit_log` - 审核日志
- `events` 扩展字段（status, is_top, published_at 等）

---

## 🚀 下一步行动

### 立即开始

1. ✅ **架构确认**（本文档）
2. 🔲 **创建 Next.js 项目**
3. 🔲 **配置 Supabase Auth**
4. 🔲 **迁移 AI 服务**

### 开发优先级

1. **P0**：登录 + AI 智能采集台
2. **P1**：内容管理 (CMS)
3. **P2**：数据看板

---

**快速参考版本**：V1.0  
**详细文档**：参见 `V3.0-产品架构总览.md`

